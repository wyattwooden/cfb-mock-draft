1. Enter in mock-draft folder on the powershell/ terminal.
2. Activate the virtual environment by running this command:
     .venv\Scripts\Activate.ps1
3. Double check dependenciesa are installed by running this command:
     pip install -r requirements.txt
4. Run the server by running this command:
     python manage.py runserver


